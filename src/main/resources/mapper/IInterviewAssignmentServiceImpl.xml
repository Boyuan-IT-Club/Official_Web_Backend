<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="club.boyuan.official.mapper.LoginMapper">

    <select id="getUserByUsername" parameterType="java.lang.String" resultType="club.boyuan.official.entity.User">
        SELECT user_id,
               username,
               password,
               email,
               role,
               name,
               phone,
               major,
               github,
               dept,
               avatar,
               create_time,
               status,
               is_member
        FROM user
        WHERE username = #{username} AND is_deleted = 0
    </select>

</mapper>