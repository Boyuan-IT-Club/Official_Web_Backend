<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="club.boyuan.official.persistence.mapper.RecruitmentCycleMapper">

    <resultMap id="RecruitmentCycleResultMap" type="club.boyuan.official.persistence.entity.RecruitmentCycle">
        <id property="cycleId" column="cycle_id"/>
        <result property="cycleName" column="cycle_name"/>
        <result property="description" column="description"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
        <result property="academicYear" column="academic_year"/>
        <result property="status" column="status"/>
        <result property="isActive" column="is_active"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 按 ID 点查刻意不过滤 is_deleted：历史简历详情、评价表都要按 ID 解析
         已删周期的名称。"已删除"只意味着从列表和开放周期里消失。 -->
    <select id="findById" parameterType="java.lang.Integer" resultMap="RecruitmentCycleResultMap">
        SELECT * FROM recruitment_cycle
        WHERE cycle_id = #{cycleId}
    </select>

    <select id="findAll" resultMap="RecruitmentCycleResultMap">
        SELECT * FROM recruitment_cycle
        WHERE is_deleted = 0
        ORDER BY created_at DESC
    </select>

    <select id="findByStatus" parameterType="java.lang.Integer" resultMap="RecruitmentCycleResultMap">
        SELECT * FROM recruitment_cycle
        WHERE status = #{status} AND is_deleted = 0
        ORDER BY created_at DESC
    </select>

    <select id="findByIsActive" parameterType="java.lang.Integer" resultMap="RecruitmentCycleResultMap">
        SELECT * FROM recruitment_cycle
        WHERE is_active = #{isActive} AND is_deleted = 0
        ORDER BY created_at DESC
    </select>

    <select id="findByAcademicYear" parameterType="java.lang.String" resultMap="RecruitmentCycleResultMap">
        SELECT * FROM recruitment_cycle
        WHERE academic_year = #{academicYear} AND is_deleted = 0
    </select>

    <!-- 软删除：单删与批删共用。同时置 is_active = 0，
         让 updateStatusBasedOnDate 等按 is_active 圈定范围的语句自然跳过它。
         这里没有硬删语句是刻意的 —— interview_session/interview_time_slot 对周期
         是 ON DELETE CASCADE，硬删会静默连带清掉整个周期的面试数据。 -->
    <update id="softDeleteByIds" parameterType="java.util.List">
        UPDATE recruitment_cycle
        SET is_deleted = 1, is_active = 0, updated_at = NOW()
        WHERE cycle_id IN
        <foreach collection="cycleIds" item="cycleId" open="(" separator="," close=")">
            #{cycleId}
        </foreach>
        AND is_deleted = 0
    </update>

    <!-- 批量更新：CASE WHEN 单语句实现（JDBC 默认禁止 foreach separator=";" 的多语句拼接） -->
    <update id="batchUpdate" parameterType="java.util.List">
        UPDATE recruitment_cycle
        SET
        cycle_name = CASE cycle_id
        <foreach collection="recruitmentCycles" item="item">
            <if test="item.cycleName != null">WHEN #{item.cycleId} THEN #{item.cycleName}</if>
        </foreach>
        ELSE cycle_name END,
        description = CASE cycle_id
        <foreach collection="recruitmentCycles" item="item">
            <if test="item.description != null">WHEN #{item.cycleId} THEN #{item.description}</if>
        </foreach>
        ELSE description END,
        start_date = CASE cycle_id
        <foreach collection="recruitmentCycles" item="item">
            <if test="item.startDate != null">WHEN #{item.cycleId} THEN #{item.startDate}</if>
        </foreach>
        ELSE start_date END,
        end_date = CASE cycle_id
        <foreach collection="recruitmentCycles" item="item">
            <if test="item.endDate != null">WHEN #{item.cycleId} THEN #{item.endDate}</if>
        </foreach>
        ELSE end_date END,
        academic_year = CASE cycle_id
        <foreach collection="recruitmentCycles" item="item">
            <if test="item.academicYear != null">WHEN #{item.cycleId} THEN #{item.academicYear}</if>
        </foreach>
        ELSE academic_year END,
        status = CASE cycle_id
        <foreach collection="recruitmentCycles" item="item">
            <if test="item.status != null">WHEN #{item.cycleId} THEN #{item.status}</if>
        </foreach>
        ELSE status END,
        is_active = CASE cycle_id
        <foreach collection="recruitmentCycles" item="item">
            <if test="item.isActive != null">WHEN #{item.cycleId} THEN #{item.isActive}</if>
        </foreach>
        ELSE is_active END,
        updated_at = NOW()
        WHERE cycle_id IN
        <foreach collection="recruitmentCycles" item="item" open="(" separator="," close=")">
            #{item.cycleId}
        </foreach>
    </update>

    <select id="findAllWithPaginationAndSorting" resultMap="RecruitmentCycleResultMap">
        SELECT * FROM recruitment_cycle
        WHERE is_deleted = 0
        ORDER BY
        <choose>
            <when test="sortBy != null and sortBy != ''">
                ${sortBy}
            </when>
            <otherwise>
                created_at
            </otherwise>
        </choose>
        <choose>
            <when test="sortOrder != null and sortOrder != ''">
                ${sortOrder}
            </when>
            <otherwise>
                DESC
            </otherwise>
        </choose>
        LIMIT #{offset}, #{limit}
    </select>

    <select id="findByConditions" resultMap="RecruitmentCycleResultMap">
        SELECT * FROM recruitment_cycle
        <where>
            AND is_deleted = 0
            <if test="cycleName != null and cycleName != ''">
                AND cycle_name LIKE CONCAT('%', #{cycleName}, '%')
            </if>
            <if test="academicYear != null and academicYear != ''">
                AND academic_year = #{academicYear}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="isActive != null">
                AND is_active = #{isActive}
            </if>
        </where>
        ORDER BY
        <choose>
            <when test="sortBy != null and sortBy != ''">
                ${sortBy}
            </when>
            <otherwise>
                created_at
            </otherwise>
        </choose>
        <choose>
            <when test="sortOrder != null and sortOrder != ''">
                ${sortOrder}
            </when>
            <otherwise>
                DESC
            </otherwise>
        </choose>
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByConditions" resultType="java.lang.Long">
        SELECT COUNT(*) FROM recruitment_cycle
        <where>
            AND is_deleted = 0
            <if test="cycleName != null and cycleName != ''">
                AND cycle_name LIKE CONCAT('%', #{cycleName}, '%')
            </if>
            <if test="academicYear != null and academicYear != ''">
                AND academic_year = #{academicYear}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="isActive != null">
                AND is_active = #{isActive}
            </if>
        </where>
    </select>

    <update id="updateStatusBasedOnDate">
        UPDATE recruitment_cycle
        SET status = CASE
            WHEN #{currentDate} &lt; start_date THEN 1  -- 未开始
            WHEN #{currentDate} BETWEEN start_date AND end_date THEN 2  -- 进行中
            WHEN #{currentDate} &gt; end_date THEN 3  -- 已结束
            ELSE status
        END,
        updated_at = NOW()
        WHERE is_active = 1 AND is_deleted = 0
    </update>

    <select id="findOpenForApplication" resultMap="RecruitmentCycleResultMap">
        SELECT * FROM recruitment_cycle
        WHERE is_active = 1
          AND is_deleted = 0
          AND #{today} BETWEEN start_date AND end_date
        ORDER BY start_date DESC, cycle_id DESC
    </select>

</mapper>
