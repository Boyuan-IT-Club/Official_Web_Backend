<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="club.boyuan.official.persistence.mapper.ResumeFieldDefinitionMapper">

    <resultMap id="ResumeFieldDefinitionMap" type="club.boyuan.official.persistence.entity.ResumeFieldDefinition">
        <id column="field_id" property="fieldId"/>
        <result column="cycle_id" property="cycleId"/>
        <result column="field_key" property="fieldKey"/>
        <result column="field_label" property="fieldLabel"/>
        <result column="field_type" property="fieldType"/>
        <result column="placeholder" property="placeholder"/>
        <result column="is_required" property="isRequired"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="is_active" property="isActive"/>
        <!-- options 是 JSON 列，必须显式声明 TypeHandler：
             实体上的 @TableField(typeHandler=...) 只作用于 BaseMapper 的查询，
             自定义 XML 的 resultMap 不会自动继承它，漏掉就永远读出 null -->
        <result column="options" property="options"
                typeHandler="com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- 自定义查询方法 -->
    <select id="findByCycleId" parameterType="java.lang.Integer" resultMap="ResumeFieldDefinitionMap">
        SELECT * FROM resume_field_definition
        WHERE cycle_id = #{cycleId} AND is_active = 1
        ORDER BY sort_order ASC
    </select>

    <select id="findById" parameterType="java.lang.Integer" resultMap="ResumeFieldDefinitionMap">
        SELECT * FROM resume_field_definition
        WHERE field_id = #{fieldId}
    </select>

</mapper>