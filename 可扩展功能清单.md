# 博源官网后端 · 可扩展功能清单

> 基于现有系统（用户/角色/权限/部门、动态字段简历、方案B 面试志愿+场次分配、活动、飞书对接、消息 outbox）梳理的待建功能与扩展建议。
> 优先级：**高** 建议先做，**中** 次之，**低** 后续增强；估算为相对工作量（小/中/大）。
> （已按需求排除：统一身份登录、面试官排班与评分表、通知偏好设置，共保留 23 项。）

## 一、简历模块

| 功能 | 说明 | 涉及模块 | 优先级 | 估算 |
| --- | --- | --- | --- | --- |
| 简历模版导出 + 附件一键导入 | 按周期字段生成 Excel/Word 模版，学生离线填写后上传，解析回填 `resume_field_value`；带逐行校验与错误行报告。 | ResumeController / ResumeFieldDefinition | 高 | 大 |
| 字段自动从个人信息同步 | 姓名/邮箱/手机/专业/年级/GitHub 等从 `user` 表预填，字段定义增加 `source_key` 映射，学生无需重复填写。 | ResumeFieldDefinition / User | 高 | 中 |
| 简历附件上传（作品集/证书） | 新增 file 字段类型，支持 PDF/图片上传与预览，交由对象存储托管。 | Resume / 存储 | 中 | 中 |
| 申请人名单批量导出 Excel | 现仅有单份 PDF 导出（`EXPORT_EXCEL` 枚举已预留），补齐按周期/状态筛选的批量 Excel。 | ResumeController | 中 | 小 |
| 字段校验规则可视化配置 | 字段定义支持必填/正则/长度/选项集，前端表单据此校验，减少脏数据。 | ResumeFieldDefinition | 中 | 中 |
| 草稿自动保存与找回 | 填写中定时存草稿、异常退出可恢复，降低填写流失。 | Resume | 低 | 中 |

## 二、面试模块（承接方案B）

| 功能 | 说明 | 涉及模块 | 优先级 | 估算 |
| --- | --- | --- | --- | --- |
| 分配后自动通知精确时间地点 | outbox 目前关闭；分配/调剂完成后经 MQ 发邮件/短信告知场次、精确时刻与地点。 | InterviewNotification / outbox | 高 | 中 |
| 分配结果同步飞书多维表 | 现有飞书对接旧 slot，改接 `session_id` 维度，把 部门×时间×地点×精确时刻 推送飞书。 | InterviewFeishu | 中 | 中 |
| 分配试运行 + 一键撤销整轮 | dry-run 预览不落库；已分配可整轮回滚（释放场次占用、撤销 schedule）。 | SessionAssignment | 中 | 小 |
| 分配可视化看板 | 各场次占用热力图、待调剂名单拖拽到有空场次，一屏完成人工调剂。 | SessionAssignment | 中 | 中 |
| 冲突检测 | 同地点同时间多场次、面试官时间冲突、容量与时窗可容纳人数等校验与告警。 | InterviewSession | 低 | 中 |

## 三、管理端

| 功能 | 说明 | 涉及模块 | 优先级 | 估算 |
| --- | --- | --- | --- | --- |
| 数据看板 / 招募漏斗统计 | 提交→评审→面试→录取漏斗、部门热度、时段热度、按周期对比。 | system / 统计 | 高 | 中 |
| 操作审计日志 | 记录谁在何时改了简历状态/权限/分配，可检索可回溯。 | system / 审计 | 中 | 中 |
| 招募周期一键克隆 | 复制字段定义、时间窗、场次模板到新周期，减少重复配置。 | RecruitmentCycle | 低 | 小 |
| 批量操作 | 批量改状态、批量发通知、批量导出，提升管理员批处理效率。 | Resume / Admin | 中 | 小 |
| 权限精细化（数据范围/字段级） | 在 RBAC 基础上增加数据范围（本部门可见）与字段级读写控制。 | Role / Permission | 低 | 大 |
| 邮件/通知模版管理 | 可视化维护通知模版（录取、未录取、面试提醒），支持变量占位替换。 | InterviewNotification | 中 | 中 |

## 四、用户端

| 功能 | 说明 | 涉及模块 | 优先级 | 估算 |
| --- | --- | --- | --- | --- |
| 申请进度时间线 | 个人中心展示 已提交→评审中→面试已排(时间地点)→结果 的进度线。 | Resume / Interview | 高 | 中 |
| 结果查询 + 站内消息 | 录取/未录取结果页与站内信，配合邮件通知形成闭环。 | InterviewResult | 中 | 小 |

## 五、平台 / 技术

| 功能 | 说明 | 涉及模块 | 优先级 | 估算 |
| --- | --- | --- | --- | --- |
| OpenAPI/Swagger 接口文档 | 自动生成 API 文档便于前后端联调与对接飞书等外部系统。 | 全局 | 中 | 小 |
| 附件对象存储（OSS/S3） | 用对象存储替代本地 uploads，支持 CDN、备份与横向扩展。 | 存储 | 中 | 中 |
| 可观测性（指标/日志/链路） | Micrometer + Prometheus 指标、结构化日志、链路追踪，便于排障。 | 全局 | 中 | 中 |
| 测试覆盖 + CI 流水线 | 补关键路径单测/集成测试，接入 CI 保证回归安全。 | 全局 | 中 | 中 |

---

## 建议落地顺序

1. 先补 **通知闭环 + 申请进度时间线**，让流程可感知；
2. 再做 **简历模版导入 + 字段自动同步**，降低填写成本；
3. 随后上 **数据看板 + 分配可视化调剂**，提升管理效率；
4. 平台类（对象存储、可观测性、CI）与业务并行推进。

## 两项重点功能落地步骤

### 简历模版导出 + 附件一键导入
1. 按周期字段定义生成带表头/占位的 Excel（或 Word）模版供下载。
2. 学生离线填好后上传附件，后端按 `field_label` / `field_key` 解析每列。
3. 逐行校验（必填、格式、选项），回填 `resume_field_value`。
4. 返回成功条数与错误行明细，支持修正后重传。

### 简历字段自动同步个人信息
1. 字段定义新增 `source_key`，映射到 `user` 属性（name/email/phone/major…）。
2. 首次进入填写页时，用当前登录用户信息预填这些字段。
3. 标记为「可编辑/只读」，允许覆盖或锁定。
4. 提交时以简历字段值为准，个人信息变更不回改历史简历。
